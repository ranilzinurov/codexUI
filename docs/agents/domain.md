# Domain Docs

This repository uses a single domain context.

- Read `CONTEXT.md` at the repository root for project language.
- ADRs, when needed, live under `docs/adr/`.
- Do not put implementation details in `CONTEXT.md`; it is a glossary for stable domain terms.

When work changes the meaning of a domain term, update `CONTEXT.md` in the same change.
